# --- Runtime -------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29            # Azure Database for PostgreSQL (Flexible Server)
aiosqlite>=0.20          # dev/test database
httpx>=0.27              # Google OAuth token exchange / Drive API
anthropic>=0.116         # AnthropicFoundry client (Claude on Microsoft Foundry)
openai>=1.109            # AsyncAzureOpenAI client (GPT on Microsoft Foundry)
cryptography>=42         # Fernet encryption of Google OAuth tokens at rest
itsdangerous>=2.2        # HMAC-signed OAuth state
python-jose[cryptography]>=3.3  # Entra ID access-token (JWT/JWKS) verification
python-multipart>=0.0.9  # skill file uploads
pydantic>=2.7
python-dotenv>=1.0       # loads infra/.env for local dev (see app/config.py)

# --- Azure (production providers; imported lazily) --------------------------
azure-identity>=1.17
azure-keyvault-secrets>=4.8
azure-storage-blob>=12.20
azure-messaging-webpubsubservice>=1.2

# --- Tests -------------------------------------------------------------------
pytest>=8.2
pytest-asyncio>=0.23
